---
# Role: sshd_hostbasedauthentication_disabled
# Description: Handlers for sshd_hostbasedauthentication_disabled role

- name: Restart SSH Service
  ansible.builtin.systemd:
    name: "{{ sshd_service_name }}"
    state: restarted
    daemon_reload: true
