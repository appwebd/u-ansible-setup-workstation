---
# defaults/main.yml
# Default variables for sshd_hostbasedauthentication_disabled role
# These values can be overridden by inventory or extra vars

sshd_config_file: /etc/ssh/sshd_config
sshd_hostbasedauthentication_value: "no"
