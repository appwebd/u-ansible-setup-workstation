---
# defaults/main.yml
# Role         : disable_tipc
# Description  : Ensure TIPC kernel module is disabled (Wazuh ID 35605)
#
# Default configuration for the `disable_tipc` role

tipc_modprobe_conf_path: /etc/modprobe.d/tipc.conf
tipc_install_line: "install tipc /bin/false"
