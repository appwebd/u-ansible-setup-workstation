---
# defaults/main.yml
# Default variables for disable_tipc role
tipc_modprobe_conf_file: /etc/modprobe.d/tipc.conf
tipc_blacklist_content: |
  #
  # Description : Ensure tipc kernel module is not available.
  # Wazuh ID    : 35505
  # Generated   : {{ lookup('pipe', 'date "+%Y-%m-%d_%H:%M:%S"') }}
  #
  install tipc /bin/false
  blacklist tipc
