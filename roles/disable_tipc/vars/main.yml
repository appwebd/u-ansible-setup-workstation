---
# vars/main.yml
# Role         : disable_tipc_module
# Description  : Ensure TIPC kernel module is disabled (Wazuh ID 35605)
#
# No additional variables needed — all logic derived from defaults and runtime checks
