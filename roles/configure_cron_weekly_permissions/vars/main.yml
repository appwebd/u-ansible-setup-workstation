---
# vars/main.yml
# Internal constants for the configure_cron_weekly_permissions role
# These are not intended to be overridden by users

cron_weekly_permissions:
  owner: root
  group: root
  mode: "0700"
