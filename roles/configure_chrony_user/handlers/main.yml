---
# Role: configure_chrony_user
# Description: Handlers for the configure_chrony_user role

- name: Restart chronyd
  ansible.builtin.systemd:
    name: "{{ chronyd_service_name | default('chronyd') }}"
    state: restarted
    daemon_reload: true
