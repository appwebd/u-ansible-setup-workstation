---
# defaults/main.yml
# Default variables for sshd_ignore_rhosts role
# These values can be overridden by inventory or playbook variables

sshd_config_file: /etc/ssh/sshd_config
sshd_service_name: sshd
sshd_ignore_rhosts_value: "yes"
