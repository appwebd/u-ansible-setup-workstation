---
# Role: sshd_maxsessions
# Description: Handlers for sshd_maxsessions role

- name: Restart sshd
  ansible.builtin.systemd:
    name: "{{ sshd_service_name }}"
    state: restarted
    daemon_reload: true
