---
# defaults/main.yml
# Default variables for the configure_cron_monthly_permissions role
# These values can be overridden by inventory or playbook variables

cron_monthly_path: /etc/cron.monthly
cron_monthly_owner: root
cron_monthly_group: root
cron_monthly_mode: "0700"
