---
# defaults/main.yml
# Default variables for the install_sudo role
# These are safe defaults and can be overridden by inventory or playbook variables

sudo_package_name: "sudo"
sudo_package_state: "present"
