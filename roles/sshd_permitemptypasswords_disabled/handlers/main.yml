---
# Role: sshd_permitemptypasswords_disabled
# Description: Handlers for the sshd_permitemptypasswords_disabled role

- name: Restart sshd
  ansible.builtin.systemd:
    name: "{{ sshd_service_name }}"
    state: restarted
    daemon_reload: true
