---
# defaults/main.yml
# Default variables for sshd_permitemptypasswords_disabled role
# These values can be overridden by inventory or play variables

sshd_config_file: /etc/ssh/sshd_config
sshd_permit_empty_passwords_value: "no"
