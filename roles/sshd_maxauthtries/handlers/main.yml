---
# Role: sshd_maxauthtries
# Description: Handlers for sshd_maxauthtries role

- name: Restart sshd
  ansible.builtin.systemd:
    name: "{{ sshd_service_name }}"
    state: restarted
    daemon_reload: true
