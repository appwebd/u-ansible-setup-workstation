---
# Role: sshd_client_alive
# Description: Handlers for the sshd_client_alive role

- name: Restart sshd
  ansible.builtin.systemd:
    name: "{{ sshd_service_name }}"
    state: restarted
    daemon_reload: true
