---
# vars/main.yml
# Internal constants for the disable_xinetd role
# These are not intended to be overridden by users

xinetd_unit_names:
  - "{{ xinetd_service_name }}.service"
  - "{{ xinetd_service_name }}.socket"
