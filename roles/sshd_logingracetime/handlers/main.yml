---
# Role: sshd_logingracetime
# Description: Handlers for the sshd_logingracetime role

- name: Restart sshd
  ansible.builtin.systemd:
    name: "{{ sshd_service_name }}"
    state: restarted
    daemon_reload: true
